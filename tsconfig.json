{
   "extends": "expo/tsconfig.base",
   "compilerOptions": {
      "strict": true,
      "baseUrl": ".", // ✅ Needed for paths to work
      "paths": {
         "@/*": ["./*"] // 👈 point directly to the root folder
      }
   },
   "exclude": ["node_modules"],
   // "include": ["**/*.ts", "**/*.tsx"],
   "include": ["**/*.ts", "**/*.tsx", "**/*.js", "**/*.jsx"]
}
