{
   "extends": "expo/tsconfig.base",
   "compilerOptions": {
      "strict": true,
      "baseUrl": ".", // ✅ Needed for paths to work
      "paths": {
         "@/*": ["app/*"] // 👈 point directly to the app folder
      }
   },
   "exclude": ["node_modules"],
   // "include": ["**/*.ts", "**/*.tsx"],
   "include": ["**/*.ts", "**/*.tsx", "**/*.js", "**/*.jsx"]
}
